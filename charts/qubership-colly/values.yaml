---
colly:
  image: ghcr.io/netcracker/qubership-colly:latest
  serviceName: qubership-colly
  instancesRepo:
  cronSchedule: 0 0/20 * * * ?
  db:
    host:
    username: postgres
    password: postgres
  ports:
    http: 8080
  ingress:
    http:
      annotations:
